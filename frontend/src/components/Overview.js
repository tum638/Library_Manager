import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
    flex: 2;
    background-color: rgba(255, 255, 255, .5);
    align-items: center;
    justify-content: center;
`

const MiddlePanel = () => {
  return (
      <Container>MiddlePanel</Container>
  )
}

export default MiddlePanel